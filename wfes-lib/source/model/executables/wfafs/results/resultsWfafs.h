#ifndef RESULTSWFAFS_H
#define RESULTSWFAFS_H

#include <QStandardPaths>
#include <QTextStream>
#include <QDir>

#include <boost/math/special_functions/fpclassify.hpp>

#include "utils/types.h"

/**
 * @brief The ResultsWfafs class encapsulates the results generated by an
 * execution of wfafs.
 */
class ResultsWfafs {
    public:
        /**
         * @brief Execution time.
         */
        double time;

        /**
         * @brief Probabilities calculated.
         */
        dvec probs;

        /**
         * @brief String containing possible errors during execution.
         * This is later shown to the user in a MessageDialog.
         */
        std::string error;

        /**
         * @brief Empty constructor of ResultsWfafs.
         */
        ResultsWfafs();

        /**
         * @brief Constructor of ResultsWfafs with execution time.
         * @param time Execution time.
         */
        ResultsWfafs(double time);

        /**
         * @brief Constructor of ResultsWfafs when an error happens during execution.
         * @param error Description of the error or message generated by the error.
         */
        ResultsWfafs(std::string error);

        /**
         * @brief Constructor of ResultsWfafs that returns probs to generate a chart.
         * @param probs Probabilities calculated.
         * @param time Execution time.
         */
        ResultsWfafs(dvec probs, double time);

        /**
         * @brief Write results in a CSV file.
         * @param results results to be printed in a CSV file.
         * @param name Name of the file.
         */
        void writeResultsToFile(ResultsWfafs *results, std::string name);
};

#endif // RESULTSWFAFS_H
