#ifndef RESULTSWFAFLE_H
#define RESULTSWFAFLE_H

#include <QStandardPaths>
#include <QDebug>
#include <QDir>

#include <boost/math/special_functions/fpclassify.hpp>

#include "utils/types.h"

/**
 * @brief The ResultsWfafle class encapsulates the results generated by an
 * execution of wfafle.
 */
class ResultsWfafle {
    public:
        /**
         * @brief Execution time.
         */
        double time;

        /**
         * @brief Probabilities calculated.
         */
        dvec probs;

        /**
         * @brief String containing possible errors during execution.
         * This is later shown to the user in a MessageDialog.
         */
        std::string error;

        /**
         * @brief Empty constructor of ResultsWfafle.
         */
        ResultsWfafle();

        /**
         * @brief Constructor of ResultsWfafle with execution time.
         * @param time Execution time.
         */
        ResultsWfafle(double time);

        /**
         * @brief Constructor of ResultsWfafle when an error happens during execution.
         * @param error Description of the error or message generated by the error.
         */
        ResultsWfafle(std::string error);

        /**
         * @brief Constructor of ResultsWfafle that returns probs to generate a chart.
         * @param probs Probabilities calculated.
         * @param time Execution time.
         */
        ResultsWfafle(dvec probs, double time);

        /**
         * @brief Write results in a CSV file.
         * @param results results to be printed in a CSV file.
         * @param name Name of the file.
         */
        void writeResultsToFile(ResultsWfafle *results, std::string name);
};

#endif // RESULTSWFAFLE_H
