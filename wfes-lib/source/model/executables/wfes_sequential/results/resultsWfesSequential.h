#ifndef RESULTSWFESSEQUENTIAL_H
#define RESULTSWFESSEQUENTIAL_H

#include <QStandardPaths>
#include <QTextStream>
#include <QDir>

#include <boost/math/special_functions/fpclassify.hpp>

#include "utils/types.h"

#include <model/executables/wfes_sequential/config/configWfesSequential.h>

/**
 * @brief The ResultsWfesSequential class encapsulates the results generated by an
 * execution of wfes sequential.
 */
class ResultsWfesSequential {
    public:
        /**
         * @brief Probability of extinction.
         */
        double pExt;

        /**
         * @brief Probability of fixation.
         */
        double pFix;

        /**
         * @brief Probability of time out.
         */
        double pTmo;

        /**
         * @brief Expected number of generations until extinction.
         */
        double tExt;

        /**
         * @brief Standard deviation of expected number of generations until extinction.
         */
        double tExtStd;

        /**
         * @brief Expected number of generations until fixation.
         */
        double tFix;

        /**
         * @brief Standard deviation of expected number of generations until fixation.
         */
        double tFixStd;

        /**
         * @brief Expected number of generations until time out.
         */
        double tTmo;

        /**
         * @brief Standard deviation of expected number of generations until time out.
         */
        double tTmoStd;

        /**
         * @brief Execution time.
         */
        double time;

        /**
         * @brief String containing possible errors during execution.
         * This is later shown to the user in a MessageDialog.
         */
        std::string error;

        /**
         * @brief Empty constructor of ResultsWfesSequential.
         */
        ResultsWfesSequential();

        /**
         * @brief Constructor of ResultsWfesSequential with execution time.
         * @param time Execution time.
         */
        ResultsWfesSequential(double time);

        /**
         * @brief Constructor of ResultsWfesSequential when an error happens during execution.
         * @param error Description of the error or message generated by the error.
         */
        ResultsWfesSequential(std::string error);

        /**
         * @brief Constructor of ResultsWfesSequential for execution.
         * @param pExt Probability of extinction.
         * @param pFix Probability of fixation.
         * @param pTmo Probability of time out.
         * @param tExt Expected number of generations until extinction.
         * @param tExtStd Standard deviation of expected number of generations until extinction.
         * @param tFix Expected number of generations until fixation.
         * @param tFixStd Standard deviation of expected number of generations until fixation.
         * @param tTmo Expected number of generations until time out.
         * @param tTmoStd Standard deviation of expected number of generations until time out.
         * @param time Execution time.
         */
        ResultsWfesSequential(double pExt, double pFix, double pTmo, double tExt, double tExtStd, double tFix, double tFixStd, double tTmo, double tTmoStd, double time);

        /**
         * @brief Write results in a CSV file.
         * @param results results to be printed in a CSV file.
         * @param name Name of the file.
         */
        void writeResultsToFile(ResultsWfesSequential *results, std::string name);

};

#endif // RESULTSWFESSEQUENTIAL_H
