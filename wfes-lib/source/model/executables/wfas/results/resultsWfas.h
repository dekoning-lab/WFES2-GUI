#ifndef RESULTSWFAS_H
#define RESULTSWFAS_H

#include <QStandardPaths>
#include <QTextStream>
#include <QDir>

#include <boost/math/special_functions/fpclassify.hpp>

#include "utils/types.h"

/**
 * @brief The ResultsWfas class encapsulates the results generated by an
 * execution of wfas.
 */
class ResultsWfas {
    public:
        /**
         * @brief Execution time.
         */
        double time;

        /**
         * @brief Probabilities calculated.
         */
        dvec probs;

        /**
         * @brief String containing possible errors during execution.
         * This is later shown to the user in a MessageDialog.
         */
        std::string error;

        /**
         * @brief Empty constructor of ResultsWfas.
         */
        ResultsWfas();

        /**
         * @brief Constructor of ResultsWfas with execution time.
         * @param time Execution time.
         */
        ResultsWfas(double time);

        /**
         * @brief Constructor of ResultsWfas when an error happens during execution.
         * @param error Description of the error or message generated by the error.
         */
        ResultsWfas(std::string error);

        /**
         * @brief Constructor of ResultsWfas that returns probs to generate a chart.
         * @param probs Probabilities calculated.
         * @param time Execution time.
         */
        ResultsWfas(dvec probs, double time);

        /**
         * @brief Write results in a CSV file.
         * @param results results to be printed in a CSV file.
         * @param name Name of the file.
         */
        void writeResultsToFile(ResultsWfas *results, std::string name);
};

#endif // RESULTSWFAS_H
